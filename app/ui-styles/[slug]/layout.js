/**
 * Layout for /ui-styles/[slug]  renders the demo page fullscreen
 * without the global site Header so the demo feels self-contained.
 */
export default function UIStyleLayout({ children }) {
  return children;
}
